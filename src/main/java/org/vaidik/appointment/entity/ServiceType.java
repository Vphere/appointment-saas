package org.vaidik.appointment.entity;

public enum ServiceType {
    FIXED,          // Fixed duration appointment (e.g. haircut 30 min)
    CONSULTATION    // Open/flexible consultation (e.g. doctor, lawyer)
}